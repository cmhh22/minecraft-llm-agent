# 🎮 Demo: Lolita AI Bot in Action

> Real examples from actual gameplay sessions using **100% free AI models**.

---

## 🤖 Bot Profile

| Setting | Value |
|---------|-------|
| **Name** | Lolita |
| **Language** | Spanish |
| **Model** | `mistral-ai/mistral-medium-2505` via GitHub Models |
| **Cost** | $0 (150 requests/day free) |
| **Mode** | God Mode (Creative) |
| **Minecraft** | Java 1.21.6 |

---

## 💬 Example 1: First Contact

The bot spawns and immediately introduces herself:

```
[System] → "Respond with hello world and your name"
[Lolita] → "¡Hola mundo! Soy Lolita."
```

**Player interaction:**
```
[cmhh]   → "acercate a mi"
[Lolita] → !goToPlayer("cmhh", 2)
[System] → "Teleported to cmhh."
[Lolita] → "¡Aquí estoy! 😊"
```

---

## 🏠 Example 2: Building a House

**Player says:** `"construye una casa"`

The bot generates and executes this code autonomously:

```javascript
// Step 1: Define house dimensions
const width = 7, depth = 7, height = 5;
const pos = bot.entity.position;

// Step 2: Cobblestone foundation
for (let x = pos.x; x < pos.x + width; x++)
    for (let z = pos.z; z < pos.z + depth; z++)
        await skills.placeBlock(bot, "cobblestone", x, pos.y - 1, z);

// Step 3: Oak plank walls with glass windows
for (let y = pos.y; y < pos.y + height; y++)
    for (let x = pos.x; x < pos.x + width; x++)
        for (let z = pos.z; z < pos.z + depth; z++)
            if (/* is wall */) {
                if (/* window position */)
                    await skills.placeBlock(bot, "glass_pane", x, y, z);
                else
                    await skills.placeBlock(bot, "oak_planks", x, y, z);
            }

// Step 4: Oak log corner pillars
// Step 5: Peaked roof with oak_stairs
// Step 6: Oak door on front wall
// Step 7: Interior: torch, crafting_table, furnace, bed
// Step 8: Brick chimney
```

**Result:** A 7×7×5 house with cobblestone foundation, oak plank walls, glass windows, log corner pillars, peaked roof, front door, chimney, and furnished interior.

**Bot response:**
```
[Lolita] → "¡Listo! Tu casa está terminada. 😊
(La casa tiene cimientos de adoquín, paredes de roble, ventanas de vidrio, 
un techo inclinado, una chimenea de ladrillos, una puerta, una cama, 
una mesa de crafteo y un horno. ¡Espero que te guste!)"
```

### House Features:
- ✅ Cobblestone foundation layer
- ✅ Oak plank walls with glass pane windows
- ✅ Oak log structural pillars at 4 corners
- ✅ Oak door entrance
- ✅ Peaked roof with stairs blocks
- ✅ Interior: torch, crafting table, furnace, bed
- ✅ Brick chimney

---

## 🏰 Example 3: Building a Medieval Castle

**Player says:** `"construye un castillo ahora"`

The bot generates a much larger construction (95,767 chars of build output):

```javascript
// Castle specifications generated by the AI:
const castleWidth = 20;
const castleDepth = 20;
const wallHeight = 10;
const towerHeight = 15;

// Construction sequence:
// 1. Stone foundation (20×20)
// 2. Cobblestone outer walls with brick battlements
// 3. Three towers with brick construction and cone roofs
// 4. Flags on tower tops (oak_fence + red_wool)
// 5. Entrance with drawbridge (oak_planks + chains)
// 6. Interior: torches, crafting area, bed, furniture
// 7. Water moat surrounding the entire castle
```

### Castle Features:
- ✅ 20×20 stone foundation
- ✅ 10-block high cobblestone walls
- ✅ Brick battlement crenellations
- ✅ 3 towers (15 blocks tall) with cone roofs
- ✅ Flags on top of each tower
- ✅ Drawbridge with chains at entrance
- ✅ Furnished interior (torches, crafting, beds)
- ✅ Water moat surrounding the castle

---

## 🧠 How It Works

```
┌─────────────┐     ┌──────────────────┐     ┌─────────────┐
│  Minecraft   │────▶│   Mindcraft Bot   │────▶│  GitHub AI   │
│   Server     │◀────│   (Mineflayer)    │◀────│  Models API  │
└─────────────┘     └──────────────────┘     └─────────────┘
     Game              Code Generation          LLM Inference
   Actions             & Execution              (FREE Tier)
```

1. **Player** sends chat message → `"construye una casa"`
2. **Bot** receives message via Mineflayer
3. **LLM** (Mistral Medium) generates JavaScript code
4. **Bot** executes code block-by-block in Minecraft
5. **Bot** responds in Spanish with a summary

---

## 📊 Model Performance Comparison

| Model | Provider | Cost | Build Quality | Speed | Notes |
|-------|----------|------|---------------|-------|-------|
| `mistral-medium-2505` | GitHub Models | Free | ⭐⭐⭐⭐ | Medium | **Recommended** - best free option |
| `mistral-small-2503` | GitHub Models | Free | ⭐⭐⭐ | Fast | Good for simple tasks |
| `gemini-2.5-flash-lite` | Google | Free | ⭐⭐⭐ | Fast | Rate limited (500/day) |
| `stepfun/step-3.5-flash` | OpenRouter | Free | ⭐⭐ | Fast | 50 req/day limit |

---

## 🎯 Bot Capabilities

| Category | Examples |
|----------|----------|
| **Chat** | Conversations in Spanish, context-aware responses |
| **Navigation** | Go to player, go to coordinates, follow players |
| **Building** | Houses, castles, any structure from natural language |
| **Mining** | Collect specific blocks, dig tunnels |
| **Crafting** | Craft items from recipes |
| **Combat** | Fight mobs, defend player |
| **Survival** | Eat food, manage inventory, equip gear |

---

## 🚀 Try It Yourself

```bash
# 1. Clone the project
git clone https://github.com/cmhh22/minecraft-llm-agent.git

# 2. Install dependencies
cd minecraft-llm-agent
npm install

# 3. Add your free API key
# Copy keys.example.json to keys.json and add your GitHub PAT

# 4. Open Minecraft 1.21.6, create world, Open to LAN (port 55916)

# 5. Start the bot
node main.js

# 6. Chat with Lolita in-game!
```

---

*All examples shown are from real gameplay sessions. No pre-scripted behavior.*
